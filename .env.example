# ===========================================
# ARQUIVO DE EXEMPLO - VARIÁVEIS DE AMBIENTE
# ===========================================
# Copie este arquivo para .env e preencha com seus valores reais
# Comando: cp .env.example .env (Linux/Mac) ou copy .env.example .env (Windows)

# ===========================================
# CONFIGURAÇÕES DO N8N
# ===========================================
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http

# Autenticação do n8n (usuário/senha para acessar interface)
N8N_USERNAME=user
N8N_PASSWORD=password

# Chave de criptografia do n8n (mantenha este valor para compatibilidade)
N8N_ENCRYPTION_KEY=6b5e1d6b83e34c6a9f03c3e93a9e5f19

# ===========================================
# CONFIGURAÇÕES DO POSTGRES (OPCIONAL)
# ===========================================
# Se quiser usar banco Postgres local em vez do Supabase
POSTGRES_DB=taskmanager
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# ===========================================
# AUTENTICAÇÃO JWT
# ===========================================
# Chave secreta para assinar tokens JWT (mantenha este valor)
JWT_SECRET=9a2d8f6d7c1340b68f621aa2c3d49e41

# ===========================================
# SUPABASE (OBRIGATÓRIO)
# ===========================================
# Substitua pelos valores do seu projeto Supabase
# Como obter: https://supabase.com > seu projeto > Settings > API

# URL do projeto Supabase
SUPABASE_URL=sua_url_do_supabase

# Chave pública anônima
SUPABASE_ANON_KEY=sua_anon_key_do_supabase

# Chave de serviço (service role)
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key_do_supabase